<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
  android:layout_width="wrap_content"
  android:layout_height="wrap_content"
  android:layout_gravity="center"
  android:background="@drawable/toast_background"
  android:gravity="center"
  android:orientation="vertical"
  android:paddingLeft="@dimen/toast_padding_left_and_right"
  android:paddingTop="@dimen/toast_padding_top_and_bottom"
  android:paddingRight="@dimen/toast_padding_left_and_right"
  android:paddingBottom="@dimen/toast_padding_top_and_bottom">

  <ImageView
    android:id="@+id/toast_icon"
    android:layout_width="@dimen/toast_icon_size"
    android:layout_height="@dimen/toast_icon_size"
    android:layout_marginBottom="@dimen/toast_icon_margin_bottom"
    android:background="@null"
    android:visibility="gone" />

  <TextView
    android:id="@+id/toast_text"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:background="@null"
    android:ellipsize="end"
    android:gravity="center"
    android:lineSpacingExtra="@dimen/toast_line_spacing_extra"
    android:maxWidth="295dp"
    android:maxLines="2"
    android:textColor="@android:color/white"
    android:textSize="@dimen/toast_text_size" />
</LinearLayout>